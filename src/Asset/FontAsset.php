<?php

namespace Core\Asset;

// Used by the Document loader and may be referenced during Style compilation
// Create a system to notify admin on missing assets
// Use a fallback font, and eventually system-ui on missing

class FontAsset {}
